<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>cloud.xcan.angus</groupId>
    <artifactId>xcan-infra.security</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>xcan-infra.auth-open2p-starter</artifactId>
  <name>AngusInfra :: Security :: Open2P Authorization </name>
  <packaging>jar</packaging>
  <description>An auto-configured library for private authorization (OAuth2-client_credentials Scheme).</description>

  <properties>
    <xcan-angusgm.api.version>1.0.0</xcan-angusgm.api.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>cloud.xcan.angus</groupId>
      <artifactId>xcan-infra.core</artifactId>
    </dependency>

    <!-- TODO 移除、替换成独立实现类 -->
    <dependency>
      <groupId>cloud.xcan.sdf</groupId>
      <artifactId>xcan-angusgm.api</artifactId>
      <version>${xcan-angusgm.api.version}</version>
    </dependency>
  </dependencies>

</project>
